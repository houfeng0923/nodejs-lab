{
    "rootPath": "{{ROOT_PATH}}",
    "rules": {
         //"default": function (req, res, next) {
         //    var err = new Error('Not Found');
         //    err.status = 404;
         //    next(err);
         //},
         "default": "desktop",
         "desktop": function (ua,req) {
             return req.ua.type == "desktop";
         },
         "mobile": function (ua,req) {
             return req.ua.type == "phone";
         }
    }
}